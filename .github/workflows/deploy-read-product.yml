name: Deploy ReadProduct to AWS EC2 (Test)

on:
  push:
    branches:
      - test  # Se ejecuta cuando se sube código a la rama "test"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Conectar a la instancia EC2 y actualizar contenedor
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: ${{ secrets.AWS_EC2_HOST }}
        username: ${{ secrets.AWS_EC2_USER }}
        key: ${{ secrets.AWS_EC2_KEY }}
        script: |
          # Asegurar permisos para Docker en EC2
          sudo usermod -aG docker $USER
          newgrp docker
          
          # Iniciar sesión en Docker Hub
          docker login -u "${{ secrets.DOCKERHUB_USER }}" -p "${{ secrets.DOCKERHUB_PASSWORD }}"
          
          # Detener y eliminar el contenedor anterior si existe
          docker stop read-product-container || true
          docker rm read-product-container || true
          
          # Descargar la última imagen desde Docker Hub
          docker pull erickuce/read-product-service:latest
          
          # Ejecutar el nuevo contenedor con variables de entorno
          docker run -d -p 8007:8007 --env-file .env --name read-product-container erickuce/read-product-service
          
          # Verificar si está corriendo
          docker ps
